import { useCallback, useState } from 'react';\nimport { apiClient } from '../services/api';\nimport { Schedule, TripSearchFilters } from '../types/api';\n\nexport const useTripSearch = () => {\n  const [trips, setTrips] = useState<Schedule[]>([]);\n  const [loading, setLoading] = useState(false);\n  const [error, setError] = useState<string | null>(null);\n\n  const search = useCallback(async (filters: TripSearchFilters) => {\n    try {\n      setLoading(true);\n      setError(null);\n      const results = await apiClient.searchTrips(filters);\n      setTrips(results);\n    } catch (err) {\n      setError('Failed to search trips');\n      console.error(err);\n    } finally {\n      setLoading(false);\n    }\n  }, []);\n\n  return { trips, loading, error, search };\n};\n\nexport const useTripDetails = (scheduleId?: number) => {\n  const [schedule, setSchedule] = useState<Schedule | null>(null);\n  const [seats, setSeats] = useState<any[]>([]);\n  const [loading, setLoading] = useState(false);\n  const [error, setError] = useState<string | null>(null);\n\n  const fetchDetails = useCallback(async (id: number) => {\n    try {\n      setLoading(true);\n      setError(null);\n      const { schedule: scheduleData, seats: seatsData } = await apiClient.getTripDetails(id);\n      setSchedule(scheduleData);\n      setSeats(seatsData);\n    } catch (err) {\n      setError('Failed to fetch trip details');\n      console.error(err);\n    } finally {\n      setLoading(false);\n    }\n  }, []);\n\n  if (scheduleId && !schedule) {\n    fetchDetails(scheduleId);\n  }\n\n  return { schedule, seats, loading, error, fetchDetails };\n};\n