import { useCallback, useState } from 'react';\nimport { apiClient } from '../services/api';\nimport { Booking, BookingRequest } from '../types/api';\n\nexport const useBookings = () => {\n  const [bookings, setBookings] = useState<Booking[]>([]);\n  const [loading, setLoading] = useState(false);\n  const [error, setError] = useState<string | null>(null);\n\n  const fetchBookings = useCallback(async (status?: 'upcoming' | 'past') => {\n    try {\n      setLoading(true);\n      setError(null);\n      const results = await apiClient.getBookings(status);\n      setBookings(results);\n    } catch (err) {\n      setError('Failed to fetch bookings');\n      console.error(err);\n    } finally {\n      setLoading(false);\n    }\n  }, []);\n\n  const createBooking = useCallback(async (data: BookingRequest) => {\n    try {\n      setLoading(true);\n      setError(null);\n      const booking = await apiClient.createBooking(data);\n      setBookings([...bookings, booking]);\n      return booking;\n    } catch (err) {\n      setError('Failed to create booking');\n      console.error(err);\n      throw err;\n    } finally {\n      setLoading(false);\n    }\n  }, [bookings]);\n\n  const cancelBooking = useCallback(async (bookingId: number) => {\n    try {\n      setLoading(true);\n      setError(null);\n      await apiClient.cancelBooking(bookingId);\n      setBookings(bookings.filter(b => b.id !== bookingId));\n    } catch (err) {\n      setError('Failed to cancel booking');\n      console.error(err);\n      throw err;\n    } finally {\n      setLoading(false);\n    }\n  }, [bookings]);\n\n  return { bookings, loading, error, fetchBookings, createBooking, cancelBooking };\n};\n\nexport const usePayment = () => {\n  const [loading, setLoading] = useState(false);\n  const [error, setError] = useState<string | null>(null);\n\n  const getPaymentPayload = useCallback(async (bookingId: number) => {\n    try {\n      setLoading(true);\n      setError(null);\n      return await apiClient.getPaymentPayload(bookingId);\n    } catch (err) {\n      setError('Failed to get payment payload');\n      console.error(err);\n      throw err;\n    } finally {\n      setLoading(false);\n    }\n  }, []);\n\n  const confirmPayment = useCallback(async (bookingId: number, transactionRef: string) => {\n    try {\n      setLoading(true);\n      setError(null);\n      return await apiClient.confirmPayment(bookingId, transactionRef);\n    } catch (err) {\n      setError('Failed to confirm payment');\n      console.error(err);\n      throw err;\n    } finally {\n      setLoading(false);\n    }\n  }, []);\n\n  return { loading, error, getPaymentPayload, confirmPayment };\n};\n